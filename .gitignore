node_modules
dist
dist-server
dist-nomock

# Secrets and generated credentials — never commit these.
.env
.env.local
.env.*.local
invites.local.txt

# Editor / OS
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json

# Generated by npm run db:export-sql. No secrets, but a build artifact.
setup.import.sql

# Deploy archives from npm run deploy:bundle.
deploy/

# Private team list (real names and login emails) — copy team.example.ts to create it.
server/scripts/team.local.ts

# TypeScript incremental build info.
*.tsbuildinfo

# Internal work summaries — not for the public repository.
docs/summary-*.html
docs/*Summary*.pdf
